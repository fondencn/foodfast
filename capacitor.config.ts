import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.foodfast.app',
  appName: 'foodfast',
  webDir: 'dist/foodfast/browser'
};

export default config;
